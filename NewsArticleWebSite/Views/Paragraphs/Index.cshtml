@model IEnumerable<NewsArticleWebSite.Models.Paragraph>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Article.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Article.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParagraphId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParagraphId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParagraphId })
        </td>
    </tr>
}

</table>
